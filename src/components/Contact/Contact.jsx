import React from "react";
import '../Contact/Contact.scss';

const Contact = () => {
    return (
        <section id="contact">
            <div className="contact"></div>
        </section>
    );
};

export default Contact;